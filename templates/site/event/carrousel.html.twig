<div class="carrousel">
    {% for item in items %}
    <div>
        <img src={{'/themes/'~ configs.site.theme ~'/images/accueil/pic01.jpg'}} />
        <h2>{{ item.libelle }}</h2>
        {{ item.resume|raw }}
    </div>
    {% endfor %}
</div>